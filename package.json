{
  "name": "auctionloom-root",
  "version": "1.0.0",
  "private": true,
  "description": "AuctionLoom Real-Time Auction Engine",
  "scripts": {
    "build": "npm --prefix backend install",
    "start": "npm --prefix backend start"
  }
}
